name: Update zip code data

on:
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:

env:
  BUNDLE_RETRY: 3

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v7

      - name: Setup ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 'ruby'
          bundler-cache: true

      - name: Update zip code data
        run: bundle exec rake create_zip_code_data

      - name: Check for changes
        id: diff
        # The `Last updated` header line changes on every run, so ignore it
        # when deciding whether the data actually changed.
        run: |
          if git diff --quiet -I '^# Last updated:' -- data/zip.yml; then
            changed=false
          else
            changed=true
          fi
          echo "changed=$changed" >> "$GITHUB_OUTPUT"
          echo "\`data/zip.yml\` changed: \`$changed\`" >> "$GITHUB_STEP_SUMMARY"

      - name: Create pull request
        if: steps.diff.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v8
        with:
          commit-message: 'chore: 郵便番号データを更新'
          add-paths: data/zip.yml
          branch: update-zip-code-data
          reviewers: chocoby
          title: '郵便番号データを更新'
          body: |
            日本郵便が公開している最新の `ken_all.zip` から `data/zip.yml` を更新します。

            この PR は `update-zip-code-data` ワークフローによって自動作成されました。
          delete-branch: true
