module LanguageServer
  module Protocol
    module Interface
      #
      # Registration options for a {@link CodeActionRequest}.
      #
      class CodeActionRegistrationOptions
        def initialize: (document_selector: untyped, ?code_action_kinds: Array[untyped], ?resolve_provider: bool, ?work_done_progress: bool) -> void

        @attributes: Hash[Symbol, untyped]
        attr_reader attributes: Hash[Symbol, untyped]

        %a{pure}
        def document_selector: () -> untyped

        %a{pure}
        def code_action_kinds: () -> Array[untyped]

        %a{pure}
        def resolve_provider: () -> bool

        %a{pure}
        def work_done_progress: () -> bool

        def to_hash: () -> Hash[Symbol, untyped]

        def to_json: (*untyped) -> String
      end
    end
  end
end
